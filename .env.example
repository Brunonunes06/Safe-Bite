# Configurações de Ambiente
# Todas as configurações foram movidas para backend/.env.example
# Para configurar o projeto, copie backend/.env.example para backend/.env

# Para desenvolvimento local:
# 1. Copie backend/.env.example para backend/.env
# 2. Configure suas credenciais do Mercado Pago no backend/.env
# 3. Inicie o servidor backend: cd backend && npm start

# Documentação completa em backend/.env.example
